{
  "name": "detour-provider",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Detour — browser↔agent bridge for ※ tap. Element inspection, annotations, 2-way agent communication.",
  "main": "index.mjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "bippy": "^0.2.11",
    "esbuild": "^0.25.4"
  }
}
